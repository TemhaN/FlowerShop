@model Microsoft.AspNetCore.Mvc.ModelBinding.ModelStateDictionary

@{
    ViewData["Title"] = "Ошибка";
}

<h1 class="text-2xl font-bold mb-4 text-red-600">Произошла ошибка</h1>
<p class="text-gray-700">Что-то пошло не так. Попробуйте позже или свяжитесь с поддержкой.</p>

@if (Model != null && Model.Any())
{
    <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
        <ul>
            @foreach (var error in Model.Values.SelectMany(v => v.Errors))
            {
                <li>@error.ErrorMessage</li>
            }
        </ul>
    </div>
}

<a href="/" class="text-blue-600 hover:underline">Вернуться на главную</a>